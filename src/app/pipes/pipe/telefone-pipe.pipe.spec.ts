import { TelefonePipePipe } from './telefone-pipe.pipe';

describe('TelefonePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TelefonePipePipe();
    expect(pipe).toBeTruthy();
  });
});
